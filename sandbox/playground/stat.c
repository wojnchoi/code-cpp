#include<stdio.h>

int function(int a)
{
	int b;
	if(a == -1)
	b = -1;
	else
	b = 0;
	return b;
}
int main()
{
	if(!function(0) < 0 && 1) //returns non zero if file is a directory
		printf("executed\n");
	return 0;
}
